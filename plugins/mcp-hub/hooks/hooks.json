{
  "description": "Inject the mcp router skill into context at session start; probe cwd package.json for @modelcontextprotocol/* (v1 -> /mcp migrate, v2 -> /mcp route).",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/session-start.js\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
